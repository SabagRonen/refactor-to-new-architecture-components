<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sabag.ronen.refactortonewarchitecturecomponents">

    <application
        android:allowBackup="true"
        android:name=".App"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".notesList.NotesActivity"
            android:label="@string/notes_list_activity_title"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".notesDetails.NoteDetailsActivity"
            android:label="@string/note_details_activity_title"
            android:windowSoftInputMode="adjustResize"
            android:parentActivityName=".notesList.NotesActivity"/>
    </application>

</manifest>